server:
  port: 8088

spring:
  application:
    name: cloud-brain-admin
  datasource:
    url: jdbc:mysql://localhost:3306/cloud_brain_admin?useUnicode=true&characterEncoding=UTF-8
    username: root
    password: ${MYSQL_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
  kafka:
    bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: health
  tracing:
    sampling:
      probability: 1.0
