# 智慧云脑诊疗平台 - 系统结构图

## 1. 全系统拓扑

```mermaid
graph TB
    subgraph 客户端
        Doctor[医生端 Web]
        Patient[患者端 Web]
        Admin[管理端 Web]
    end

    subgraph 网关层
        Gateway[Gateway :8080<br/>路由 + JWT 校验 + 限流]
    end

    subgraph 业务服务
        Auth[Auth :8081<br/>认证授权]
        Outpatient[Outpatient :8082<br/>门诊工作台 ★核心域]
        Imaging[Imaging :8083<br/>医学影像与 AI]
        AIAssistant[AI Assistant :8084<br/>AI 助理医生]
        Scheduling[Scheduling :8085<br/>排班与队列]
        OrderRouting[OrderRouting :8086<br/>医嘱执行与路由]
        PatientSvc[Patient :8087<br/>患者端服务]
        AdminSvc[Admin :8088<br/>管理端服务]
    end

    subgraph 中间件
        MySQL[(MySQL :3306<br/>写入主库)]
        ES[(Elasticsearch :9200<br/>全文检索)]
        Kafka[(Kafka :9092<br/>事件总线)]
        Nacos[Nacos :8848<br/>服务注册/配置]
    end

    subgraph 可观测性
        Zipkin[Zipkin :9411<br/>链路追踪]
    end

    subgraph 共享层
        SharedKernel[Shared Kernel<br/>事件契约 + 枚举]
    end

    Doctor -->|HTTP| Gateway
    Patient -->|HTTP| Gateway
    Admin -->|HTTP| Gateway

    Gateway -->|路由| Auth
    Gateway -->|路由| Outpatient
    Gateway -->|路由| Imaging
    Gateway -->|WebSocket 代理| AIAssistant
    Gateway -->|路由| Scheduling
    Gateway -->|路由| OrderRouting
    Gateway -->|路由| PatientSvc
    Gateway -->|路由| AdminSvc

    Auth --> MySQL
    Outpatient --> MySQL
    Outpatient --> ES
    Imaging --> MySQL
    Scheduling --> MySQL
    OrderRouting --> MySQL
    PatientSvc --> MySQL
    AdminSvc --> MySQL

    Outpatient -->|事件| Kafka
    Imaging -->|事件| Kafka
    AIAssistant -->|事件| Kafka
    Scheduling -->|事件| Kafka
    OrderRouting -->|事件| Kafka

    Auth --> Nacos
    Outpatient --> Nacos
    Imaging --> Nacos
    AIAssistant --> Nacos
    Scheduling --> Nacos
    OrderRouting --> Nacos
    PatientSvc --> Nacos
    AdminSvc --> Nacos

    Outpatient -.->|Zipkin 追踪| Zipkin
    OrderRouting -.->|Zipkin 追踪| Zipkin

    SharedKernel -.->|Maven 依赖| Auth
    SharedKernel -.->|Maven 依赖| Outpatient
    SharedKernel -.->|Maven 依赖| Imaging
    SharedKernel -.->|Maven 依赖| AIAssistant
    SharedKernel -.->|Maven 依赖| Scheduling
    SharedKernel -.->|Maven 依赖| OrderRouting

    style Outpatient fill:#2e7d32,color:#fff
    style Auth fill:#1565c0,color:#fff
    style OrderRouting fill:#6a1b9a,color:#fff
    style Gateway fill:#e65100,color:#fff
```

---

## 2. DDD 限界上下文映射

```mermaid
graph LR
    subgraph 通用域
        Auth[认证授权<br/>User, Role]
    end

    subgraph 核心域
        Outpatient[门诊工作台<br/>Consultation, EMR]
    end

    subgraph 支撑域
        Imaging[医学影像与 AI<br/>ImagingTask, AIReport]
        AIAssistant[AI 助理医生<br/>DiagnosticSession]
        Scheduling[排班与队列<br/>Schedule, Queue]
        OrderRouting[医嘱执行与路由<br/>MedicalOrder]
    end

    subgraph 外部系统
        Pharmacy[药房管理]
        Lab[检查检验科室]
    end

    Auth -- "共享内核\nJWT principal" --> Outpatient
    Auth -- "共享内核" --> Imaging
    Auth -- "共享内核" --> AIAssistant
    Auth -- "共享内核" --> Scheduling
    Auth -- "共享内核" --> OrderRouting

    Outpatient -- "同步 RPC\n医嘱确认" --> OrderRouting
    Outpatient -- "WebSocket\n语音流+推送" --> AIAssistant
    Outpatient -- "Kafka 事件\nQueueUpdated" --> Scheduling

    Imaging -- "Kafka 事件\nCtReportGenerated" --> Outpatient
    AIAssistant -- "Kafka 事件\nConsultationAiSummary" --> Outpatient
    Scheduling -- "Kafka 事件\nQueueUpdated" --> Outpatient

    OrderRouting -- "防腐层 ACL\nKafka 事件" --> Pharmacy
    OrderRouting -- "防腐层 ACL\nKafka 事件" --> Lab

    style Outpatient fill:#2e7d32,color:#fff
    style Auth fill:#1565c0,color:#fff
    style OrderRouting fill:#6a1b9a,color:#fff
```

---

## 3. 通信矩阵

| 源 → 目标 | 方式 | 通道 | 场景 |
|---|---|---|---|
| 客户端 → Gateway | HTTP | — | 所有前端请求入口 |
| Gateway → 各服务 | HTTP 路由 | Nacos 寻址 | 请求转发 |
| Gateway → AI Assistant | WebSocket 代理 | Nacos | 语音流直通 |
| Outpatient → OrderRouting | **同步 RPC** | OpenFeign | 医嘱确认（3s 超时） |
| Outpatient → Kafka | Outbox 异步 | `outpatient-consultation-events` | 接诊开始/结束 |
| OrderRouting → Kafka | Outbox 异步 | `medical-order-events` | 医嘱路由/完成/驳回 |
| Imaging → Kafka | Outbox 异步 | `medical-imaging-events` | AI 报告生成 |
| AI Assistant → Kafka | 异步 | `ai-assistant-events` | 会话摘要归档 |
| Scheduling → Kafka | Outbox 异步 | `doctor-scheduling-events` | 队列变更 |
| 门诊前端 ↔ AI Assistant | **WebSocket** | STOMP | 实时语音流 + 建议推送 |
| Outpatient → ES | Outbox 同步 | Kafka `emr-data-change-events` | EMR 全文索引 |
| 各服务 → Nacos | HTTP 心跳 | — | 服务注册/发现 |
| 各服务 → Zipkin | HTTP 上报 | OpenTelemetry | 链路追踪 |

---

## 4. 核心事件流（一次完整就诊）

```mermaid
sequenceDiagram
    participant Doctor as 医生
    participant Outpatient as 门诊工作台
    participant AI as AI 助理
    participant Order as 医嘱路由
    participant Imaging as 医学影像
    participant Kafka as Kafka

    Doctor->>Outpatient: 叫号接诊
    Outpatient->>Outpatient: WAITING → IN_PROGRESS
    Outpatient-->>Kafka: ConsultationStartedEvent
    Kafka-->>AI: 初始化会话上下文

    rect rgb(240,255,240)
        Note over Doctor,AI: WebSocket 实时通道
        Doctor--)AI: 语音流上行
        AI--)Doctor: 鉴别诊断推送
    end

    Doctor->>Outpatient: 开具 CT 检查
    Outpatient->>Order: POST /api/orders (同步 RPC)
    Order->>Order: 持久化 → ACCEPTED
    Order-->>Outpatient: {orderId, status:"accepted"}
    Order-->>Kafka: OrderIssuedEvent
    Kafka-->>Imaging: 触发影像分析

    Imaging->>Imaging: AI 推理 + 生成报告
    Imaging-->>Kafka: CtReportGeneratedEvent
    Kafka-->>Outpatient: 报告通知
    Outpatient-->>Doctor: 红点/弹窗提示

    Doctor->>Outpatient: 结束就诊
    Outpatient->>Outpatient: IN_PROGRESS → FINISHED
    Outpatient-->>Kafka: ConsultationFinishedEvent
    Kafka-->>AI: 触发摘要生成
    AI-->>Kafka: ConsultationAiSummaryEvent
    Kafka-->>Outpatient: 摘要写入病历
```

---

## 5. 数据存储分布

```mermaid
graph TB
    subgraph MySQL
        AuthDB[(cloud_brain_auth<br/>user, role, refresh_token)]
        OutpatientDB[(cloud_brain_outpatient<br/>consultation, emr, outpatient_outbox)]
        ImagingDB[(cloud_brain_imaging<br/>imaging_task, imaging_report, imaging_outbox)]
        SchedulingDB[(cloud_brain_scheduling<br/>schedule, queue)]
        OrderDB[(cloud_brain_order_routing<br/>medical_order, order_outbox)]
        PatientDB[(cloud_brain_patient<br/>预留)]
        AdminDB[(cloud_brain_admin<br/>预留)]
    end

    subgraph Elasticsearch
        EmrIndex[(emr 索引<br/>主诉/现病史/体格检查)]
        ReportIndex[(ai_report 索引<br/>病灶特征/定量参数)]
    end

    subgraph 对象存储
        MinIO[(MinIO / 本地挂载<br/>DICOM / NIfTI 文件)]
    end

    OutpatientDB -->|Outbox → Kafka → 同步| EmrIndex
    ImagingDB -->|Outbox → Kafka → 同步| ReportIndex
    ImagingDB -->|路径 + MD5 引用| MinIO
```

---

## 6. 项目模块一览

| 模块 | 端口 | 领域类型 | 代码状态 | 核心聚合根 |
|---|---|---|---|---|
| **shared-kernel** | — | 类库 | ✅ 完成 | 3 枚举 + 10 事件 |
| **gateway** | 8080 | 网关 | ✅ 完成 | 9 路由 + JWT 校验 |
| **auth** | 8081 | 通用域 | ✅ 完成 | User, Role |
| **outpatient** | 8082 | ★核心域 | ✅ 完成 | Consultation, EMR |
| **imaging** | 8083 | 支撑域 | ❌ 空壳 | ImagingTask, AIReport |
| **ai-assistant** | 8084 | 支撑域 | ❌ 空壳 | DiagnosticSession |
| **scheduling** | 8085 | 支撑域 | ❌ 空壳 | Schedule, Queue |
| **order-routing** | 8086 | 支撑域 | ✅ 完成 | MedicalOrder |
| **patient** | 8087 | 通用域 | 🔒 骨架 | 预留 |
| **admin** | 8088 | 通用域 | 🔒 骨架 | 预留 |
